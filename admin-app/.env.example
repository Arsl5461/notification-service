# Copy to .env and adjust if your API runs on a different host/port.
# Without this, the app uses relative /api (same host as the dev server, e.g. localhost:3000).
REACT_APP_API_URL=http://localhost:8000
